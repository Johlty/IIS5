name: Echo Message Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say a message
        run: echo "👋 Hello from GitHub Actions!"
